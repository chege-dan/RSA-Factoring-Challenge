#ifndef _FACTORS_H
#define _FACTORS_H

#include <stdio.h>
#include <stdlib.h>

void get_fact(unsigned long long int num);
ssize_t getline(char **lineptr, size_t *n, FILE *stream);

#endif /* _FACTORS_H */
